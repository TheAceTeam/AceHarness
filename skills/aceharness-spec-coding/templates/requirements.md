# 需求文档：<功能名称>

## 简介

<用 2-4 句话说明背景、目标用户、业务价值和本次范围边界。>

## 输入解读

- **用户原话/目标：** <把用户请求改写成明确行为，不丢失硬约束。>
- **硬性要求：** <必须发生的行为，例如硬失败、必须透传、必须兼容。>
- **非目标边界：** <本次明确不处理的相关范围。>
- **成功判定：** <用户或系统如何判断这件事完成。>

## 代码证据

| 证据 | 文件/函数/接口 | 说明 | 影响的需求 |
| --- | --- | --- | --- |
| E1 | <path:function or 待勘探:query> | <当前代码事实或需要确认的问题> | R1 |
| E2 | <path:function or 待勘探:query> | <当前代码事实或需要确认的问题> | R2 |

## 能力拆分

- **C1 <能力名>**: <可独立验收的行为/结果；说明来源是用户约束还是代码证据。>
- **C2 <能力名>**: <可独立验收的行为/结果；说明输入、输出和失败路径。>

## 术语表

- **<术语>**: <业务对象、状态、权限、数据或边界定义>
- **<术语>**: <定义>

## 需求

### 需求 R1：<能力/行为名称>

**能力边界：** <明确包含什么、不包含什么、输入输出是什么。>

**证据来源：** <E1/E2 或用户硬性要求。>

**用户故事：** 作为<角色>，我希望<目标>，以便<价值>。

#### 验收标准
1. WHEN <主路径触发条件> THEN <可观察结果>
2. WHEN <边界/异常/兼容条件> THEN <可观察结果>

### 需求 R2：<能力/行为名称>

**能力边界：** <明确范围。>

**证据来源：** <E1/E2 或用户硬性要求。>

**用户故事：** 作为<角色>，我希望<目标>，以便<价值>。

#### 验收标准
1. WHEN <主路径触发条件> THEN <可观察结果>
2. WHEN <边界/异常/兼容条件> THEN <可观察结果>

## 非目标

- <本次明确不做的能力、场景或技术范围>

## 待确认项

- <会影响实现策略、验收标准、兼容或风险的问题>
