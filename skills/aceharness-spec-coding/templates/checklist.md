# 问题清单 (CHECKLIST)

> 人工审批或 supervisor 审查时必须处理未勾选问题。
> 使用 `- [ ] 问题内容`，已回答标记为 `- [x]`。

- [ ] 是否已确认所有 R 编号需求都有可验证 WHEN/THEN 场景？
- [ ] 是否已确认所有 D 编号决策都有理由和替代方案？
- [ ] 是否已确认所有 leaf task 都有需求追踪、设计追踪、动作、交付和验证？
- [ ] 是否存在会影响兼容、权限、安全、性能、迁移或回退的未决问题？
- [ ] 修订时是否保留了仍有效的任务状态、`spec-coding-task` 注释和 workflow step 绑定？
