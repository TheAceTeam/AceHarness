# 议场与工作流群聊迁移计划（已归档）

> **状态：已废弃（2026-07-28）。** 本文将工作流运行绑定到 Agora 议场、多 Agent 群聊和工作流目录的迁移方案已被移除。它不描述当前产品行为，也不能作为新的实现或兼容依据。

当前工作流运行通过普通对话和状态机运行视图呈现；不再创建工作流 Agora 房间、工作流群聊绑定或 `workflow-monitor` 插件状态。轻量工作流仍是单步骤状态机配置，任务编排由 `aceharness-tasklist` 驱动。

现行设计、执行顺序和验收状态见 [轻量工作流替换任务清单](tasklists/lightweight-workflow-replacement/README.md)。保留本文仅供追溯已废弃方案。
